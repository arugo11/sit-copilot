---
name: checkpoining
description: Typo-tolerant alias for /checkpointing. Execute the same Codex-native checkpoint workflow without Claude dependencies.
---

# Checkpoining (Alias)

This command is an alias of `/checkpointing`.

## Workflow

1. Execute the exact same steps as `.codex/skills/checkpointing/SKILL.md`.
2. Preserve command contract: `/checkpoining` (alias of `/checkpointing`).

## Command Contract

- `/checkpoining`
