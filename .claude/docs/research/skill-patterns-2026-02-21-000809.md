# Skill Pattern Analysis (from checkpoint 2026-02-21-000809)

## Checkpoint Stats

- Branch: `main`
- Commits: 8
- Files changed: 0
- Codex consultations: 1
- Gemini researches: 0
- Agent Teams sessions: 5
- Tasks completed: 7/28
- Teammate logs analyzed: 15

## Candidate Patterns

## 1) `review-hardening-close-loop`

- Description:
  - 実装後に `team-review` を実行し、Medium/Low まで同一ターンで hardening を完了させる閉ループ運用。
- Trigger phrases (JA):
  - 「レビューして、指摘をすぐ反映して」
  - 「Medium+Low もまとめて直して」
- Trigger phrases (EN):
  - "Run review and apply findings now"
  - "Fix medium and low findings in one pass"
- Confidence: 0.86
- Evidence:
  - 直近セッションで `team-review` 実施後に hardening 実装と再テストまで完了。
  - `DESIGN.md` の changelog に review hardening 追記が連続している。
- Existing skill overlap:
  - `team-review` と部分重複（レビューはあるが、即時 hardening の実装手順は未固定）。

## 2) `owner-lane-team-implement`

- Description:
  - lane ごとに所有ファイルを固定し、lane単位で `ruff/ty/pytest` を通してから全体 merge gate を実行する。
- Trigger phrases (JA):
  - 「担当を分けて実装して」
  - 「レーンごとに検証して進めたい」
- Trigger phrases (EN):
  - "Implement with strict ownership lanes"
  - "Validate each lane before global checks"
- Confidence: 0.90
- Evidence:
  - 実装で lane table を先出しし、lane別の focused checks を実施。
  - 全体 gate 失敗を既存 `.claude/hooks` 由来として切り分けて報告。
- Existing skill overlap:
  - 既存 `owner-lane-team-implement` / `team-implement` と高重複。

## 3) `planning-research-merge-gate`

- Description:
  - `/startproject` で scope/evidence/interfaces/gates/risks を凍結し、承認後に `/team-implement` へ接続する。
- Trigger phrases (JA):
  - 「根拠付きで設計を固めてから実装したい」
  - 「Merge Gate を先に通したい」
- Trigger phrases (EN):
  - "Freeze design with evidence before implementation"
  - "Run merge gate before coding"
- Confidence: 0.84
- Evidence:
  - `f1-speech-token-issuance` で codebase/research/plan 3点を作成後に実装。
  - Merge Gate 項目（Scope/Evidence/Interfaces/Quality/Risks）を明示して承認待ちにした。
- Existing skill overlap:
  - 既存 `startproject` / `planning-research-merge-gate` と重複。

## 4) `checkpoint-pattern-mining-loop`

- Description:
  - `checkpoint -> analyze-prompt -> skill-patterns-{timestamp}.md` を同一 timestamp で保存し、セッション資産を再利用可能パターンとして蓄積する。
- Trigger phrases (JA):
  - 「チェックポイント取って」
  - 「今回のパターンを抽出して」
- Trigger phrases (EN):
  - "Run checkpointing"
  - "Extract reusable skill patterns from this session"
- Confidence: 0.88
- Evidence:
  - `.claude/checkpoints/2026-02-21-000809.md` と `.analyze-prompt.md` が同時生成。
  - 既存 `skill-patterns-*.md` が時系列で蓄積されている。
- Existing skill overlap:
  - 既存 `checkpointing` と高重複（品質ゲートの運用強化余地あり）。

## Decision: Create New Skill vs Strengthen Existing Skill

- Primary decision: **strengthen existing skills**.
- Rationale:
  - 候補の大半は既存 `team-review`, `team-implement`, `startproject`, `checkpointing` の運用深化でカバー可能。
  - 新規スキル追加より、既存スキルに quality gate と hardening loop を追加する方が低コスト高効果。
- Secondary decision:
  - `review-hardening-close-loop` は再発頻度が高ければ新規スキル化を検討（現時点では `team-review` 強化を優先）。

## Top 3 Recommended Actions (Impact / Effort)

1. `team-review` を強化（High impact / Low effort）
- Add: 「ユーザーが修正希望時は Medium+Low を一括修正する実装チェックリスト」を固定化。

2. `checkpointing` を強化（Medium impact / Low effort）
- Add: `skill-patterns-{timestamp}.md` の必須章チェック（Stats/3+patterns/Decision/Top actions）をスクリプト後検証に追加。

3. `team-implement` を強化（Medium impact / Medium effort）
- Add: lane 完了条件に「review findings 同期済み」を明文化し、post-review hardening lane を定義可能にする。
