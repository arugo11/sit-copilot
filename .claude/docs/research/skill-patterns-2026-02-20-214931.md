# Skill Pattern Analysis (from checkpoint 2026-02-20-214931)

## Checkpoint Stats

- Commits: 0
- Files changed: 0
- Codex consultations: 1
- Agent Teams sessions: 5
- Tasks completed: 7/28
- Teammate logs analyzed: 15

## Candidate Patterns (confidence >= 0.6)

## 1) `planning-research-merge-gate`

- Description: Architect と Researcher を並列実行し、実装前に設計を 1 つの合意文書へ統合する。
- Trigger phrases (JA):
  - 「設計と調査を並行で進めて」
  - 「実装前に根拠付きで設計を固めたい」
- Trigger phrases (EN):
  - "Run architecture and research in parallel"
  - "Finalize design with evidence before implementation"
- Workflow:
  1. Architect が API/モジュール/タスク分解を提案。
  2. Researcher がライブラリ制約・落とし穴を検証。
  3. `DESIGN.md` に統合（決定・リスク・TODO）。
  4. 実装開始条件（merge gate）を明示して開始。
- Confidence: 0.86
- Evidence:
  - `sprint1-settings-api-and-db` と `f4-lecture-qa` のログで、Architect + Researcher の分担と統合フローが繰り返し出現。
  - checkpoint summary に Codex consultation + Agent Teams 複合が確認される。
- Existing skill overlap:
  - `startproject` に強く重なる。
  - 推奨: `startproject` に merge gate テンプレートを標準化。

## 2) `owner-lane-team-implement`

- Description: 実装を担当レーン（DB/Schema/API/Test など）に分離し、競合を減らして並列で進める。
- Trigger phrases (JA):
  - 「担当レーンを分けて並列実装して」
  - 「競合しない形で分担したい」
- Trigger phrases (EN):
  - "Implement in ownership lanes"
  - "Parallelize without merge conflicts"
- Workflow:
  1. レーン定義（担当者・所有ファイル・完了条件）。
  2. 各レーンで実装 + ローカル検証。
  3. 統合前に lint/type/test ゲート。
  4. 統合後に全体テスト。
- Confidence: 0.80
- Evidence:
  - `sprint1-implement`（`db-models`, `schema-service`, `api-tests`）のタスク構造。
  - `f4-lecture-qa-implement`（`foundation/core/ai/orchestrator/tester`）の役割分担。
- Existing skill overlap:
  - `team-implement` に重なる。
  - 推奨: 所有レーン表と統合ゲートを `team-implement` に追加。

## 3) `grounded-qa-service-playbook`

- Description: QA を「索引構築 -> 取得 -> 回答 -> 検証 -> 永続化」で段階構築し、no-source フォールバックを初期から固定する。
- Trigger phrases (JA):
  - 「根拠付き QA を段階的に実装して」
  - 「先に no-source フォールバックを固めたい」
- Trigger phrases (EN):
  - "Build grounded QA in phases"
  - "Lock fallback policy first"
- Workflow:
  1. Retrieval/Answerer/Verifier の Protocol 分離。
  2. 索引作成と検索サービスを先に実装。
  3. 回答・検証を接続。
  4. `QATurn` へ監査向けメタデータ保存。
  5. no-source の deterministic fallback を保証。
- Confidence: 0.88
- Evidence:
  - `f4-lecture-qa` / `f4-lecture-qa-implement` のログで上記構成が明示。
  - verifier claim-by-claim、fallback、feature-based persistence の反復記述。
- Existing skill overlap:
  - `team-implement` + `team-review` と部分重複。
  - 推奨: QA特化サブプレイブックとして独立化を検討。

## 4) `checkpoint-pattern-mining-loop`

- Description: セッション終了時に checkpoint を取り、活動ログから次のスキル候補を抽出して research に保存する。
- Trigger phrases (JA):
  - 「チェックポイント取って」
  - 「再利用パターンを抽出して」
- Trigger phrases (EN):
  - "Create a checkpoint"
  - "Mine reusable patterns"
- Workflow:
  1. checkpoint script 実行。
  2. artifacts + `CLAUDE.md` 更新を検証。
  3. `.analyze-prompt.md` を読み、候補を抽出。
  4. 既存 skill と重複判定し、create/extend を提案。
- Confidence: 0.77
- Evidence:
  - 本 checkpoint で `.md` + `.analyze-prompt.md` 生成と session history 更新を確認。
- Existing skill overlap:
  - `checkpointing` そのもの。
  - 推奨: 出力テンプレートを既定化し、毎回同じ粒度で記録。

## Suggested Next Actions

1. `startproject` へ merge gate チェックリストを追加。
2. `team-implement` へ ownership lane テンプレートを追加。
3. `team-review` へ grounded QA 向けチェックリストを追加。
4. `checkpointing` に pattern report 自動保存を標準化。
