# Skill Pattern Analysis (from checkpoint 2026-02-20-225248)

## Checkpoint Stats

- Commits: 2
- Files changed: 0
- Codex consultations: 1
- Gemini researches: 0
- Agent Teams sessions: 5
- Tasks completed: 7/28
- Teammate logs analyzed: 15

## Candidate Patterns

## 1) `planning-research-merge-gate`

- Description: 設計（Architect）と調査（Researcher）を並列で進め、実装前に merge gate で一本化する。
- Trigger phrases (JA):
  - 「設計と調査を並行で進めて」
  - 「根拠付きで設計を固めてから実装したい」
- Trigger phrases (EN):
  - "Run planning and research in parallel"
  - "Finalize design with evidence before implementation"
- Confidence: 0.85
- Evidence:
  - `sprint0-planning`/`sprint1-implement`/`auc-0-95-improvement` で Architect + Researcher の分担が反復。
  - checkpoint の Agent Teams 構成が設計先行の運用を示す。
- Existing skill overlap:
  - 既存 `startproject` / `planning-research-merge-gate` と重複。

## 2) `owner-lane-team-implement`

- Description: 実装をレーン（DB/Schema/API/Test 等）に分解し、所有ファイルを固定して並列実装する。
- Trigger phrases (JA):
  - 「担当を分けて実装して」
  - 「競合を避けて並列で進めたい」
- Trigger phrases (EN):
  - "Implement with ownership lanes"
  - "Parallelize implementation without conflicts"
- Confidence: 0.81
- Evidence:
  - `sprint1-implement` の `db-models`/`schema-service`/`api-tests` 分担。
  - `f4-lecture-qa-implement` の `foundation/core/ai/orchestrator/tester` 分担。
- Existing skill overlap:
  - 既存 `team-implement` / `owner-lane-team-implement` と重複。

## 3) `grounded-qa-service-playbook`

- Description: QAを `index -> retrieval -> answer -> verify -> persist` の段階で固め、no-source fallback を契約化する。
- Trigger phrases (JA):
  - 「根拠必須のQAにしたい」
  - 「no-source fallback を先に固定したい」
- Trigger phrases (EN):
  - "Build grounded QA in phases"
  - "Enforce deterministic no-source fallback"
- Confidence: 0.88
- Evidence:
  - Codex consultation が BM25 + Azure OpenAI lecture QA architecture を直接要求。
  - `f4-lecture-qa-implement` のタスク内訳が index/retrieval を基点に構成。
- Existing skill overlap:
  - 既存 `grounded-qa-service-playbook` と重複。

## 4) `checkpoint-pattern-mining-loop`

- Description: セッション終端で checkpoint を生成し、分析プロンプトから再利用可能スキル候補を抽出・保存する。
- Trigger phrases (JA):
  - 「チェックポイント取って」
  - 「作業パターンを抽出して」
- Trigger phrases (EN):
  - "Run checkpointing"
  - "Extract reusable skill patterns"
- Confidence: 0.79
- Evidence:
  - `.claude/checkpoints/2026-02-20-225248.md` と `.analyze-prompt.md` が生成済み。
  - `CLAUDE.md` の Session History が追記されている。
- Existing skill overlap:
  - 既存 `checkpointing` と重複（運用ガイド強化向け）。

## Recommendation

- 新規スキル追加より、既存スキルの運用テンプレート強化（merge gate チェックリスト、ownership lane 表、checkpoint 出力規約）を優先する。
