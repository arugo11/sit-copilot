# Skill Pattern Analysis (from checkpoint 2026-02-20-221652)

## Checkpoint Stats

- Commits: 0
- Files changed: 0
- Codex consultations: 1
- Gemini researches: 0
- Agent Teams sessions: 5
- Tasks completed: 7/28
- Teammate logs analyzed: 15

## Candidate Patterns (confidence >= 0.6)

## 1) `planning-research-merge-gate`

- Description: Architect と Researcher を並列で進め、実装前に設計と根拠を 1 つに統合する。
- Trigger phrases (JA):
  - 「設計と調査を並行で進めて」
  - 「実装前に根拠付きで設計を固めたい」
- Trigger phrases (EN):
  - "Run planning and research in parallel"
  - "Finalize design with evidence before implementation"
- Workflow:
  1. Architect が API/モジュール/タスク分解を設計。
  2. Researcher がライブラリ制約と落とし穴を検証。
  3. `DESIGN.md` へ統合し、リスクと判断根拠を明記。
  4. merge gate 合意後に実装開始。
- Confidence: 0.86
- Evidence:
  - `sprint1-settings-api-and-db` と `f4-lecture-qa` で、Architect + Researcher の往復が繰り返し出現。
  - checkpoint の Codex consultation + Agent Teams activity が同フローを補強。
- Existing skill overlap:
  - `startproject` と強く重複。
  - 推奨: `startproject` に merge gate チェックリストを標準追加。

## 2) `owner-lane-team-implement`

- Description: 実装を担当レーン（DB/Schema/API/Test）に分割し、競合を抑えて並列実行する。
- Trigger phrases (JA):
  - 「担当を分けて並列実装して」
  - 「競合を避けて進めたい」
- Trigger phrases (EN):
  - "Implement in ownership lanes"
  - "Parallelize implementation without conflicts"
- Workflow:
  1. レーン定義（担当者・所有ファイル・完了条件）。
  2. 各レーンで実装 + 局所テスト。
  3. 統合前に lint/type/test ゲート。
  4. 統合後に全体回帰確認。
- Confidence: 0.80
- Evidence:
  - `sprint1-implement`（`db-models`, `schema-service`, `api-tests`）の分担構造。
  - `f4-lecture-qa-implement`（`foundation/core/ai/orchestrator/tester`）の役割分離。
- Existing skill overlap:
  - `team-implement` と重複。
  - 推奨: `team-implement` に所有レーン表テンプレートを追加。

## 3) `grounded-qa-service-playbook`

- Description: QA を「索引 -> 取得 -> 回答 -> 検証 -> 永続化」で段階構築し、no-source fallback を固定する。
- Trigger phrases (JA):
  - 「根拠必須でQAを組みたい」
  - 「no-source fallback を先に固めたい」
- Trigger phrases (EN):
  - "Build grounded QA in phases"
  - "Lock deterministic no-source fallback first"
- Workflow:
  1. Retrieval/Answerer/Verifier を Protocol で分離。
  2. index/retrieval を先行実装。
  3. answer + verification を接続。
  4. QA turn メタデータを永続化。
  5. no-source fallback を必須ガードで保証。
- Confidence: 0.89
- Evidence:
  - `f4-lecture-qa` と `f4-lecture-qa-implement` に同一構造（BM25, verifier, persistence, fallback）が反復。
  - checkpoint の Codex consultation が lecture QA architecture を示している。
- Existing skill overlap:
  - 専用スキルとして既存 `grounded-qa-service-playbook` が存在。
  - 推奨: このパターンは新規化ではなく既存スキル利用を優先。

## 4) `checkpoint-pattern-mining-loop`

- Description: セッション終端で checkpoint を作成し、活動ログから再利用パターンを抽出・保存する。
- Trigger phrases (JA):
  - 「チェックポイント取って」
  - 「再利用パターンを抽出して」
- Trigger phrases (EN):
  - "Create a checkpoint"
  - "Mine reusable workflow patterns"
- Workflow:
  1. checkpoint script 実行。
  2. artifacts と `CLAUDE.md` 更新を検証。
  3. `.analyze-prompt.md` から候補抽出。
  4. 既存 skill と重複判定して create/extend 提案。
- Confidence: 0.77
- Evidence:
  - 今回 `.claude/checkpoints/2026-02-20-221652.md` と `.analyze-prompt.md` が生成済み。
  - `CLAUDE.md` の session history 追記を確認。
- Existing skill overlap:
  - `checkpointing` 自体と重複。
  - 推奨: 自動保存先ファイル名の規約を固定化。

## Suggested Next Actions

1. `startproject` に merge gate テンプレートを追加。
2. `team-implement` に ownership lane テンプレートを追加。
3. grounded QA 系は新規スキル追加ではなく `grounded-qa-service-playbook` の運用を標準化。
4. `checkpointing` に pattern report の自動保存命名規則を追加。
