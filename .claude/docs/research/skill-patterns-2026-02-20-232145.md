# Skill Pattern Analysis (from checkpoint 2026-02-20-232145)

## Checkpoint Stats

- Commits: 5
- Files changed: 0
- Codex consultations: 1
- Gemini researches: 0
- Agent Teams sessions: 5
- Tasks completed: 7/28
- Teammate logs analyzed: 15

## Candidate Patterns

## 1) `grounded-qa-service-playbook`

- Description: QA を `index -> retrieval -> answer -> verify` の段階に分け、根拠必須・no-source fallback の契約を先に固定する。
- Trigger phrases (JA):
  - 「根拠必須のQAにして」
  - 「no-source fallback を先に決めたい」
- Trigger phrases (EN):
  - "Build grounded QA with source-first guarantees"
  - "Enforce deterministic no-source fallback"
- Confidence: 0.89
- Evidence:
  - Codex consultation が "lecture QA service using BM25 + Azure OpenAI" の設計要求を直接示している。
  - `f4-lecture-qa-implement` で `lecture_index_service` と `lecture_retrieval_service` が完了タスクとして反復。
- Existing skill overlap:
  - 既存 `grounded-qa-service-playbook` と高い重複。

## 2) `owner-lane-team-implement`

- Description: 実装を lane（foundation/core/ai/orchestrator/tester など）に分割し、担当固定で並列実装する。
- Trigger phrases (JA):
  - 「担当を分けて並列実装して」
  - 「競合しないレーン運用で進めたい」
- Trigger phrases (EN):
  - "Implement with ownership lanes"
  - "Parallelize implementation by fixed lanes"
- Confidence: 0.83
- Evidence:
  - `f4-lecture-qa-implement` と `sprint1-implement` の両方で lane ベースの役割分割が確認できる。
  - Team activity で複数ロールにタスクを割り振る運用が継続している。
- Existing skill overlap:
  - 既存 `team-implement` / `owner-lane-team-implement` と重複。

## 3) `planning-research-merge-gate`

- Description: Researcher と Architect の成果を先に固め、実装開始前に merge gate で設計の齟齬を潰す。
- Trigger phrases (JA):
  - 「設計と調査を並行で進めて」
  - 「根拠付きで設計を確定してから実装したい」
- Trigger phrases (EN):
  - "Run planning and research in parallel"
  - "Freeze design with evidence before coding"
- Confidence: 0.81
- Evidence:
  - `auc-0-95-improvement` で Researcher/Architect の完了実績が並列に存在。
  - 生成 checkpoint のチーム構成に、設計先行の運用パターンが繰り返し現れる。
- Existing skill overlap:
  - 既存 `startproject` / `planning-research-merge-gate` と重複。

## 4) `checkpoint-pattern-mining-loop`

- Description: checkpoint 生成後に analyze prompt を読み、再利用可能スキル候補を定型フォーマットで保存する。
- Trigger phrases (JA):
  - 「チェックポイントを取って」
  - 「作業パターンを抽出して」
- Trigger phrases (EN):
  - "Run checkpointing"
  - "Mine reusable skill patterns from this session"
- Confidence: 0.87
- Evidence:
  - `.claude/checkpoints/2026-02-20-232145.md` と `.analyze-prompt.md` が同時生成されている。
  - `CLAUDE.md` の Session History 追記と、過去の `skill-patterns-*.md` 蓄積が確認できる。
- Existing skill overlap:
  - 既存 `checkpointing`（運用定着フェーズ）。

## Recommendation

- 新規スキル追加より、既存スキルの運用テンプレート強化を優先する。
- 優先度: `checkpointing` の出力品質基準固定 > `team-implement` の lane 完了定義明文化 > `grounded-qa-service-playbook` の受け入れ条件テンプレート化。
